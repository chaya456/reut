export interface CarouselItem {
  id: number;
  beforeImg: string;
  afterImg: string;
}

export interface GalleryItem {
  id: number;
  image: string;
  title: string;
}

export interface Recommendation {
  id: number;
  name: string;
  quote: string;
}